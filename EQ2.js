// Create an object with a name, rollNo as keys and give its respective values. Add a new key address with a value and console log the whole object as well as the new address key’s value.

let student ={
    name: "Saman",
    rollNo: 36
}
student.address = "Nepal";

console.log(student);
console.log(student.address);